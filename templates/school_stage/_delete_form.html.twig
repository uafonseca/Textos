<form method="post" action="{{ path('school_stage_delete', {'id': school_stage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ school_stage.id) }}">
        <br>
    <button class="btn btn-danger"> <i class="fa fa-times"></i> Eliminar</button>
</form>
