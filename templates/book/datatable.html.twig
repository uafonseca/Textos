<div class="card">
	<div class="card-body">
		{{sg_datatables_render_html(datatable)}}
		{{sg_datatables_render_js(datatable)}}
	</div>
</div>
<script type="text/javascript">
	 $(document).on('click', '.action-details', function (event) {

            event.preventDefault();

            const scope = $(this);

            app.dialogs.create({
                url: scope.attr('href'),
                'containerFluid': true,
                'columnClass' : 'col-md-6'
            })
        });
</script>