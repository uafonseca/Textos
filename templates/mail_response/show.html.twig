{% extends 'base.html.twig' %}

{% block title %}MailResponse{% endblock %}

{% block body %}
    <h1>MailResponse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mail_response.id }}</td>
            </tr>
            <tr>
                <th>Context</th>
                <td>{{ mail_response.context }}</td>
            </tr>
            <tr>
                <th>Evaluation</th>
                <td>{{ mail_response.evaluation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('mail_response_index') }}">back to list</a>

    <a href="{{ path('mail_response_edit', {'id': mail_response.id}) }}">edit</a>

    {{ include('mail_response/_delete_form.html.twig') }}
{% endblock %}
