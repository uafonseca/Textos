{% extends 'layout.html.twig' %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}
{% block title %}Edit Slide{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-body">
            {{ include('slide/_form.html.twig', {'button_label': 'Actualizar'}) }}
            <br>
            <a href="{{ path('slide_index') }}" class="btn btn-secondary"><i class="fa fa-arrow-alt-circle-left"></i> Regresar</a>

            {{ include('slide/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
